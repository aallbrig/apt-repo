# apt-repo\n\nAPT repository for [allbctl](https://github.com/aallbrig/allbctl) — hosted on GitHub Pages.\n\n## Usage\n\n```bash\ncurl -fsSL https://aallbrig.github.io/apt-repo/KEY.gpg | sudo gpg --dearmor -o /usr/share/keyrings/allbctl.gpg\necho "deb [signed-by=/usr/share/keyrings/allbctl.gpg arch=amd64] https://aallbrig.github.io/apt-repo stable main" | sudo tee /etc/apt/sources.list.d/allbctl.list\nsudo apt update && sudo apt install allbctl\n```\n
